{
  "name": "mitosis-lab",
  "version": "0.1.0",
  "private": true,
  "description": "An interactive cell-lineage simulator — tune mutation rate and division timing and watch a lineage tree branch and grow in real time.",
  "license": "MIT",
  "author": "Charlie Krug",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.0",
    "vitest": "^1.5.0"
  }
}
